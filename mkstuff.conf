PACKAGE=qpe-gaim
VER=0.3
RELEASE=1

CVS=0

LANGUAGES="en"

TMPDIR=tmp

ARCH=arm
RM=rm
TAR=tar

# Do not edit this
if [ "$CVS" != "" -a "$CVS" != "no" -a "$CVS" != "0" ]; then
	RELEASE="0.`date +%Y%m%d`"
fi

# vim: syntax=sh
